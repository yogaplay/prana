import 'package:flutter/material.dart';

class ExploerPage extends StatelessWidget {
  const ExploerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('둘러보기 페이지'));
  }
}
